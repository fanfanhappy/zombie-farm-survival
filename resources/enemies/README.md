# 敌人资源

这里保存敌人的可视化数据配置，不保存敌人运行逻辑。

- `enemy_database.tres`：敌人总表。新增敌人配置后，需要把资源拖进它的 `enemies` 数组。
- `definitions/`：每种敌人的独立配置，包括生命、移动、仇恨、攻击、经验、颜色和随机掉落。

日常调整只需在 Godot 文件系统中双击对应 `.tres`，在检查器修改参数。不要再把敌人数值写回 `zombie_controller.gd`。
