# 敌人定义

一个 `.tres` 对应一种敌人。目前包含普通感染者与迅捷感染者。

新增敌人时复制最接近的配置并修改：

1. `enemy_id` 必须唯一，推荐使用小写英文和下划线。
2. 调整战斗、移动和外观参数。
3. 设置掉落物品 ID、概率及数量范围。
4. 把新配置加入上一级 `enemy_database.tres`。
5. 在尸潮波次或生成逻辑中使用同一个 `enemy_id`。

如果只更换敌人图片，应修改敌人场景的可视节点；这里的 `body_tint` 主要用于当前原型占位外观。
