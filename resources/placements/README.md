# 可放置物资源维护说明

- 每个定义关联一个放置类型、正式场景和物理占地尺寸。
- 正式放置与鼠标预览复用同一个场景。
- 新增可放置物时创建定义，并加入 `placeable_database.tres`。

## 新增流程

1. 先建立正式世界场景，包含外观、碰撞和必要脚本。
2. 在物品定义中设置对应 `placement_type`。
3. 创建放置定义，关联正式场景并填写接近真实碰撞范围的 `footprint`。
4. 加入总数据库后测试鼠标预览、旋转、边界校验、正式放置和存档恢复。
