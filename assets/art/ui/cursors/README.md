# cursors 目录说明

游戏鼠标指针素材目录。

- `mouse_cursor_arrow.png`：当前默认鼠标指针，原始尺寸为16×16像素，热点位于左上角 `(0, 0)`。
- 默认指针在 `project.godot` 的 `display/mouse_cursor` 设置中引用，主菜单和游戏场景共用同一外观。
- 后续重绘时可以直接替换同名图片；如指针点击位置不准，在项目设置的 `Display > Mouse Cursor > Custom Image Hotspot` 调整热点。
